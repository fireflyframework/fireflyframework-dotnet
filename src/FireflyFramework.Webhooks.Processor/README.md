# FireflyFramework.Webhooks.Processor

Pluggable webhook processor pipeline (signature validation, idempotency). Mirrors fireflyframework-webhooks-processor.

See [`docs/AUDIT.md`](../../docs/AUDIT.md) for the full module mapping and the parent-module README for the API contracts this project implements or extends.
